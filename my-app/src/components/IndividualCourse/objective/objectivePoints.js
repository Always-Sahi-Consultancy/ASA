const ObjectivePoints =(props) => {
    return (
        <div className="points-container">
            <div className="point__main">{props.point}</div>
        </div>
    );
}

export default ObjectivePoints;